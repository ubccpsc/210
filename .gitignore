.DS_Store

.idea/

node_modules/

docs/.vitepress/dist
docs/.vitepress/cache

# docs/public/diagrams is deliberately committed: the build fetches each
# diagram from kroki.io only when its source changes, so keeping the
# generated SVGs in the repo means CI does not depend on that service.
