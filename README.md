# 210

The UBC CPSC 210 textbook.

## Development

Content is written in markdown in the `docs/` directory and rendered using [vitepress](https://vitepress.dev/guide/getting-started).

You can preview your changes by running:

```sh
pnpm docs:dev
```

Any changes pushed to `main` will automatically be deployed on GitHub Pages.